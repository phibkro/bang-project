{
  "name": "@bang/root",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "type": "module",
  "scripts": {
    "demo:m000": "bun run scripts/demo-m000.ts",
    "effect:setup": "effect-tsgo patch --typescript --oxlint",
    "check:effect-lsp": "bun run scripts/check-effect-lsp.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "check": "bun run effect:setup && bun run demo:m000 && bun run check:effect-lsp && bun run test && bun run lint && bun run format:check",
    "prepare": "bun run effect:setup"
  },
  "dependencies": {
    "effect": "4.0.0-rc.108"
  },
  "devDependencies": {
    "@bang/core": "workspace:*",
    "@bang/target-effect": "workspace:*",
    "@effect/tsgo": "0.36.4",
    "@types/bun": "1.3.14",
    "oxfmt": "0.61.0",
    "oxlint": "1.77.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2"
  }
}
